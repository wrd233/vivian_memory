version: '3.8'

services:
  particle-sea:
    build: .
    ports:
      - "3001:3001"  # 后端API
      - "5173:5173"  # 前端开发服务器
    volumes:
      - ./data:/app/data  # 持久化记忆数据
    environment:
      - NODE_ENV=development
    command: npm run dev:full  # 同时启动前后端