version: '3.8'

services:
  particle-sea:
    build: .
    ports:
      - "8080:8080"  # 统一通过8080端口访问
    volumes:
      - ./data:/app/data  # 持久化记忆数据
    environment:
      - NODE_ENV=production
      - PORT=8080
    restart: unless-stopped